-- print a script that lists all records of the table second_table of the database hbtn_0c_0
-- query of script that lists all records of the table second_table
SELECT score, name FROM second_table ORDER BY score DESC;
